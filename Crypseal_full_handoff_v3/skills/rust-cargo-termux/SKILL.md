# Skill: Rust/Cargo in Termux

Use for Rust projects.

Flow:
1. Inspect Cargo.toml.
2. Verify rust/cargo installed.
3. Run `cargo check` before `cargo build`.
4. Use release builds only when needed.
5. Capture linker/Android-specific errors.

Ask before installing toolchains or native dependencies.
