# Skill: Existing Repo Repair Workflow

Flow:
- status → repo map → reproduce failure → inspect relevant files → patch → rerun → summarize diff → optional commit.

No blind edits before failure reproduction unless user requests direct change.
