# Skill: New Project Build Workflow

Flow:
- clarify if necessary → plan → create project skeleton → write files → run formatter/test/build → preview/export → commit.

Every project starts with AGENT.md and README.
