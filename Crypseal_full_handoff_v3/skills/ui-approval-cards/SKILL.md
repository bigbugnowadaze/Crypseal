# Skill: UI Approval Cards

Use for approval UI.

Must show exact action, risk, reason, cwd/path, bound files/hashes, and options: allow once, deny once, always allow pattern, always deny pattern, edit.

Approval UI must never hide command chaining, network, package install, or external storage labels.
