# Skill: Git Workflow

Use for repo state, diffs, commits, rollback.

Rules:
- Always inspect `git status` before edits if repo exists.
- Show diff before commit.
- Do not push without explicit user request.
- Commit messages summarize actual diff, not plan.
- Checkpoint independent of git.

Verification:
- dirty tree is visible to user.
